{
  "name": "euroflow",
  "version": "0.1.0",
  "description": "EuroFlow — the unified European money network. MVP backend API (zero runtime dependencies).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
